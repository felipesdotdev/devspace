# Assets

The upstream repository contains two PNG files in this directory:

- `devspace-logo-light.png` — 66092 bytes
- `devspace-screenshot.png` — 968459 bytes

The exact upstream blob sizes were verified from Git. Binary file materialization requires a byte-preserving file operation, while the available DevSpace edit/write tools accept text content.
